{
  "crons": [
    {
      "path": "/api/cron/social",
      "schedule": "0 9 */2 * *"
    }
  ],
  "ignoreCommand": "bash -c 'B=$(git rev-parse -q --verify \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" 2>/dev/null || echo HEAD^); git diff --quiet \"$B\" HEAD -- . \":(exclude)photo-library\" && exit 0; exit 1'"
}
