node_modules/
.next/
dist/
.env
.env.local
.env*.local
*.log
.DS_Store
.vercel

# TypeScript incremental build cache — regenerated by every build, so tracking it
# guarantees a conflict on every branch.
*.tsbuildinfo
